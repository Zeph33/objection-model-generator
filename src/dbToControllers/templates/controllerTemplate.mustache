/**
 * File generated with dbToControllers generator
 * @author Jonathan Diego Rodríguez Rdz. <jonathan@bquate.com>
 * @author Didier Richard. <didier@kisslog.fr>
 */

/**
 * Extends from Controller abstract class, pass the {{model}} to super needed.
 * @class
 * @extends Controller
 */
const Controller = require('{{{controllerParent}}}')
const models = require('{{{modelsPath}}}')

class {{controller}} extends Controller {

  constructor() {
    super(models.{{model}})
  }
{{#rels}}
  getByID(req, res, next) {
    super.getByID(req, res, next, models.UserModel.query().eager('[{{rels}}]'))
  }
{{/rels}}

}
const class{{controller}} = new {{controller}}()
module.exports = class{{controller}}